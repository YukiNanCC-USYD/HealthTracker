import React, { Component } from 'react';
import { Fonts } from '../../src/utils/Fonts'
import {
  SafeAreaView,
  StyleSheet,
  ScrollView,
  View,
  Text,
  // Button,
  StatusBar,
  Image,
  AsyncStorage,
  ActivityIndicator,
  TouchableOpacity
} from 'react-native';

import {
  // Header,
  LearnMoreLinks,
  Colors,
  DebugInstructions,
  ReloadInstructions,
} from 'react-native/Libraries/NewAppScreen';

import SplashScreen from 'react-native-splash-screen';
// const sleep = require('es6-sleep').promise;
import { 
  Container, 
  Header,
  Title, 
  Content, 
  Footer, 
  FooterTab, 
  Button, 
  Left, 
  Right, 
  Body, 
  Icon,
  Item,
  Label,
  Form,
  Input,
  Card,
  CardItem,
  Thumbnail
} from 'native-base';

// import { createAppContainer, createSwitchNavigator, DrawerNavigator, ThemeColors } from 'react-navigation';
// import { createStackNavigator, HeaderTitle } from './node_modules/react-navigation-stack';
// import {createDrawerNavigator } from './node_modules/react-navigation-drawer';

// import { extend } from './node_modules/dayjs';

export default class BPScreen extends React.Component{
  render() {
    return(
      <SafeAreaView>

      </SafeAreaView>
    );
  }
}